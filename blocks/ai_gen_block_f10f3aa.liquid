{% doc %}
  @prompt
    Create a Shopify hero banner section with full editor controls. The foreground image is visible on desktop only and hidden on mobile.
    
    Editable settings (Customizer):
    
    heading (text → render as UPPERCASE)
    
    subheading (richtext)
    
    button_label (text) and button_link (url)
    
    background_image (image_picker)
    
    foreground_image (image_picker) – desktop only
    
    foreground_max_width (range: 200–500px)
    
    text_align (select: left | center)
    
    overlay_opacity (range: 0–80%)
    
    text_color, button_color, button_text_color, button_hover_color
    
    content_padding_desktop (range: 20–80px)
    
    content_padding_mobile (range: 12–32px)
    
    banner_height_desktop (range: 300–800px, default 350px)
    
    banner_height_mobile (range: 200–600px, default auto)
    
    Desktop layout:
    
    Banner height is adjustable via banner_height_desktop.
    
    Background image: full-bleed cover with optional dark overlay.
    
    Content block (H1, sub, CTA) aligned per text_align.
    
    Foreground image anchored pixel-perfect to bottom-right (no margin/padding/border), constrained by foreground_max_width.
    
    Mobile layout (≤768px):
    
    Banner height is adjustable via banner_height_mobile (or auto by default).
    
    Foreground image is hidden.
    
    Stack: Heading → Sub-heading → CTA.
    
    Maintain safe padding (content_padding_mobile).
    
    Scale typography for readability (heading ~70–85% of desktop, stays UPPERCASE).
    
    CTA must be touch-friendly (min-height 44px).
    
    Accessibility & performance:
    
    Require alt text (fallback to setting label if empty).
    
    Ensure WCAG AA contrast using overlay_opacity.
    
    Use responsive image params and lazy-load noncritical media.
    
    Acceptance criteria:
    
    Foreground image shows on desktop (bottom-right, no unintended spacing) and does not appear on mobile.
    
    Banner height is fully adjustable from the theme editor for both desktop and mobile.
    
    Text is always readable; CTA is accessible.
    
    All content and visuals are editable in the Shopify customizer—no code edits needed for updates.

{% enddoc %}
{% assign ai_gen_id = block.id | replace: '_', '' | downcase %}

{% style %}
  .ai-hero-banner-{{ ai_gen_id }} {
    position: relative;
    width: 100%;
    height: {{ block.settings.banner_height_desktop }}px;
    display: flex;
    align-items: center;
    overflow: hidden;
  }

  .ai-hero-banner-background-{{ ai_gen_id }} {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
  }

  .ai-hero-banner-background-{{ ai_gen_id }} img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .ai-hero-banner-background-placeholder-{{ ai_gen_id }} {
    width: 100%;
    height: 100%;
    background-color: #f4f4f4;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
  }

  .ai-hero-banner-background-placeholder-{{ ai_gen_id }} svg {
    width: 100%;
    height: 100%;
    max-width: 300px;
    max-height: 200px;
  }

  .ai-hero-banner-empty-state-{{ ai_gen_id }} {
    position: absolute;
    bottom: 20px;
    left: 50%;
    transform: translateX(-50%);
    padding: 8px 16px;
    background-color: rgba(255, 255, 255, 0.9);
    border-radius: 4px;
    font-size: 14px;
    color: #666;
    text-align: center;
    pointer-events: none;
  }

  .ai-hero-banner-overlay-{{ ai_gen_id }} {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, {{ block.settings.overlay_opacity | divided_by: 100.0 }});
    z-index: 2;
  }

  .ai-hero-banner-content-{{ ai_gen_id }} {
    position: relative;
    z-index: 3;
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
    padding: {{ block.settings.content_padding_desktop }}px;
    {% if block.settings.text_align == 'center' %}
      text-align: center;
    {% else %}
      text-align: left;
    {% endif %}
  }

  .ai-hero-banner-heading-{{ ai_gen_id }} {
    font-size: 48px;
    font-weight: 700;
    line-height: 1.2;
    margin: 0 0 16px 0;
    color: {{ block.settings.text_color }};
    text-transform: uppercase;
  }

  .ai-hero-banner-subheading-{{ ai_gen_id }} {
    font-size: 18px;
    line-height: 1.5;
    margin: 0 0 24px 0;
    color: {{ block.settings.text_color }};
  }

  .ai-hero-banner-button-{{ ai_gen_id }} {
    display: inline-block;
    padding: 16px 32px;
    background-color: {{ block.settings.button_color }};
    color: {{ block.settings.button_text_color }};
    text-decoration: none;
    border-radius: 4px;
    font-size: 16px;
    font-weight: 600;
    transition: background-color 0.3s ease;
    min-height: 44px;
    line-height: 1.2;
  }

  .ai-hero-banner-button-{{ ai_gen_id }}:hover {
    background-color: {{ block.settings.button_hover_color }};
  }

  .ai-hero-banner-foreground-{{ ai_gen_id }} {
    position: absolute;
    bottom: 0;
    right: 0;
    z-index: 4;
    max-width: {{ block.settings.foreground_max_width }}px;
    height: auto;
  }

  .ai-hero-banner-foreground-{{ ai_gen_id }} img {
    width: 100%;
    height: auto;
    display: block;
  }

  @media screen and (max-width: 768px) {
    .ai-hero-banner-{{ ai_gen_id }} {
      {% if block.settings.banner_height_mobile == 'auto' %}
        height: auto;
        min-height: 300px;
      {% else %}
        height: {{ block.settings.banner_height_mobile }}px;
      {% endif %}
    }

    .ai-hero-banner-content-{{ ai_gen_id }} {
      padding: {{ block.settings.content_padding_mobile }}px;
    }

    .ai-hero-banner-heading-{{ ai_gen_id }} {
      font-size: 36px;
    }

    .ai-hero-banner-subheading-{{ ai_gen_id }} {
      font-size: 16px;
      margin-bottom: 20px;
    }

    .ai-hero-banner-button-{{ ai_gen_id }} {
      padding: 14px 28px;
      font-size: 15px;
    }

    .ai-hero-banner-foreground-{{ ai_gen_id }} {
      display: none;
    }
  }
{% endstyle %}

<div class="ai-hero-banner-{{ ai_gen_id }}" {{ block.shopify_attributes }}>
  <div class="ai-hero-banner-background-{{ ai_gen_id }}">
    {% if block.settings.background_image %}
      <img
        src="{{ block.settings.background_image | image_url: width: 2000 }}"
        alt="{{ block.settings.background_image.alt | default: 'Hero background' | escape }}"
        loading="lazy"
        width="{{ block.settings.background_image.width }}"
        height="{{ block.settings.background_image.height }}"
      >
    {% else %}
      <div class="ai-hero-banner-background-placeholder-{{ ai_gen_id }}">
        {{ 'hero-apparel-1' | placeholder_svg_tag }}
        <div class="ai-hero-banner-empty-state-{{ ai_gen_id }}">
          Add a background image
        </div>
      </div>
    {% endif %}
  </div>

  <div class="ai-hero-banner-overlay-{{ ai_gen_id }}"></div>

  <div class="ai-hero-banner-content-{{ ai_gen_id }}">
    {% if block.settings.heading != blank %}
      <h1 class="ai-hero-banner-heading-{{ ai_gen_id }}">{{ block.settings.heading }}</h1>
    {% endif %}

    {% if block.settings.subheading != blank %}
      <div class="ai-hero-banner-subheading-{{ ai_gen_id }}">{{ block.settings.subheading }}</div>
    {% endif %}

    {% if block.settings.button_label != blank and block.settings.button_link != blank %}
      <a href="{{ block.settings.button_link }}" class="ai-hero-banner-button-{{ ai_gen_id }}">
        {{ block.settings.button_label }}
      </a>
    {% endif %}
  </div>

  {% if block.settings.foreground_image %}
    <div class="ai-hero-banner-foreground-{{ ai_gen_id }}">
      <img
        src="{{ block.settings.foreground_image | image_url: width: 800 }}"
        alt="{{ block.settings.foreground_image.alt | default: 'Hero foreground' | escape }}"
        loading="lazy"
        width="{{ block.settings.foreground_image.width }}"
        height="{{ block.settings.foreground_image.height }}"
      >
    </div>
  {% endif %}
</div>

{% schema %}
{
  "name": "Hero banner",
  "settings": [
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Welcome to our store"
    },
    {
      "type": "richtext",
      "id": "subheading",
      "label": "Subheading",
      "default": "<p>Discover amazing products and exceptional quality</p>"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button label",
      "default": "Shop now"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button link"
    },
    {
      "type": "select",
      "id": "text_align",
      "label": "Text alignment",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        }
      ],
      "default": "left"
    },
    {
      "type": "header",
      "content": "Images"
    },
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "Background image"
    },
    {
      "type": "image_picker",
      "id": "foreground_image",
      "label": "Foreground image"
    },
    {
      "type": "range",
      "id": "foreground_max_width",
      "min": 200,
      "max": 500,
      "step": 10,
      "unit": "px",
      "label": "Foreground image max width",
      "default": 350
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "min": 0,
      "max": 80,
      "step": 5,
      "unit": "%",
      "label": "Background overlay opacity",
      "default": 30
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "button_color",
      "label": "Button color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "button_text_color",
      "label": "Button text color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "button_hover_color",
      "label": "Button hover color",
      "default": "#333333"
    },
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "range",
      "id": "banner_height_desktop",
      "min": 300,
      "max": 800,
      "step": 10,
      "unit": "px",
      "label": "Desktop height",
      "default": 350
    },
    {
      "type": "select",
      "id": "banner_height_mobile",
      "label": "Mobile height",
      "options": [
        {
          "value": "auto",
          "label": "Auto"
        },
        {
          "value": "200",
          "label": "200px"
        },
        {
          "value": "250",
          "label": "250px"
        },
        {
          "value": "300",
          "label": "300px"
        },
        {
          "value": "350",
          "label": "350px"
        },
        {
          "value": "400",
          "label": "400px"
        },
        {
          "value": "450",
          "label": "450px"
        },
        {
          "value": "500",
          "label": "500px"
        },
        {
          "value": "550",
          "label": "550px"
        },
        {
          "value": "600",
          "label": "600px"
        }
      ],
      "default": "auto"
    },
    {
      "type": "range",
      "id": "content_padding_desktop",
      "min": 20,
      "max": 80,
      "step": 4,
      "unit": "px",
      "label": "Desktop content padding",
      "default": 40
    },
    {
      "type": "range",
      "id": "content_padding_mobile",
      "min": 12,
      "max": 32,
      "step": 2,
      "unit": "px",
      "label": "Mobile content padding",
      "default": 20
    }
  ],
  "presets": [
    {
      "name": "Hero banner"
    }
  ],
  "tag": null
}
{% endschema %}